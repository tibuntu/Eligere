---
========================
To execute this role use:

ansible-playbook roles/tk.raspi.default/playbooks/provision.yml -i inventory/default -vv --vault-password-file=~/.vault_password_file

========================
Dependencies:

- ansible
  (brew install ansible)
========================
